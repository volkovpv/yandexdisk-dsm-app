# Changelog — ARM-сборка

Все изменения ARM64-версии пакета. Базовый пакет — x86_64
`0.1.6.1080-3` (ветка `main`).

## Версионирование

С версии **1.0.0** ARM-сборка ведёт собственную линию версий по правилам
**семантического версионирования** `МАЖОР.МИНОР.ПАТЧ`:

- **МАЖОР** (первая цифра) — крупные/несовместимые изменения;
- **МИНОР** (вторая цифра) — новая функциональность с обратной совместимостью;
- **ПАТЧ** (третья цифра) — исправления ошибок.

Изменение любого разряда **обнуляет все разряды правее** (напр. `1.4.7` →
минор → `1.5.0`; → мажор → `2.0.0`). DSM-суффикс сборки (`-N`) больше не
используется: повторный выпуск без изменения исходников — это ПАТЧ.

Версию для каждой задачи **определяет ИИ, выполняющий задачу**, исходя из
характера изменений.

## [2.0.3] (ARM) — 2026-06-20

ПАТЧ: кнопка «Синхронизировать сейчас» падала с «Некорректный ответ сервера», и из UI
не работала ни одна мутация (sync, set-folder, set-token, check-folder — отсюда же
«Владелец: —» на дашборде), при том что прогон по Планировщику задач DSM шёл штатно.
Причина: при ВКЛЮЧЁННОЙ защите от CSRF (Панель управления → Безопасность) webman режет
POST к `/webman/3rdparty/.../settings.cgi` СВОЕЙ страницей **403 (HTML)** ещё ДО CGI,
если запрос не несёт валидный DSM `SynoToken`. А `main.js` доставал токен послойно
(`SYNO.SDS.Session.getToken()` → `window._SYNO_TOKEN` → cookie) — в этой сборке DSM **все
три источника пусты**, токен не слался, и `r.json()` давился HTML-страницей 403 →
«Некорректный ответ сервера» (это JSON-parse-фолбэк, единственное место строки). Прогон
по Планировщику от webman/CSRF не зависит — потому и работал. Диагностировано в DevTools:
POST `action=sync` → 403, HTML, 35 мс, без `SynoToken`; прямой вызов CGI по SSH — успешен
(значит 403 от webman, не от скрипта); `SYNO.SDS.Session.getToken()` пуст; CSRF-защита в
DSM включена.

### Fixed
- **`main.js` (`synoToken`): токен берётся с авторитетного эндпоинта DSM**
  `/webman/login.cgi?enable_syno_token=yes` (по сессионной cookie, БЕЗ логина/пароля
  отдаёт `{"SynoToken":…,"success":true}`; подтверждено на DSM 7), кэшируется на жизнь
  страницы и шлётся на КАЖДЫЙ POST (`&SynoToken=` + заголовок `X-SYNO-TOKEN`). Прежние
  быстрые источники (SDS-API/глобаль/cookie) оставлены фолбэками. Тем же токеном закрыт
  `clear_log.cgi` (очистка истории — тоже POST под CSRF). Это и был баг.
- **`settings.cgi do_sync` + `main.js syncNow`: «Синхронизировать сейчас» — fire-and-forget.**
  sync запускается ОТВЯЗАННЫМ от запроса процессом (`setsid` → `nohup` → подоболочка,
  закрытый stdin, вывод в `/dev/null`), CGI сразу отдаёт `{"ok":true,"message":"Синхронизация
  запущена"}`, а UI лишь СТАРТУЕТ прогон и НЕ ждёт его (полный `bisync` большой папки идёт
  минутами — окну незачем висеть со спиннером): показывает «запущена, идёт в фоне», результат
  видно позже при переоткрытии окна. Так же ведёт себя запуск из Планировщика задач. Контракт
  `sync.state` (7 полей) и состав действий CGI НЕ менялись.
- **`main.js`: GET-чтения CGI идут с `cache:"no-store"`** (`status.cgi`/`log.cgi`/`get-config`)
  — у `status.cgi` нет анти-кэш заголовков, и без этого браузер мог подсунуть из кэша старое
  состояние при перечитывании.

### Tests (ОТДЕЛЬНЫМ PR — по канону правки `test/` не идут с проверяемым кодом)
- `check-ui-contract.sh` §5 научить отличать системные эндпоинты DSM
  (`/webman/<name>.cgi`, напр. `login.cgi`) от пакетных CGI — иначе ссылка на `login.cgi`
  ложно требует файл в пакете.
- Регрессы: POST-мутация без `SynoToken` отклоняется (403), а с токеном — проходит;
  `action=sync` отвечает мгновенно и не ждёт долгий прогон. Проверено вручную: pre-flight
  POST с токеном из `login.cgi` снимает 403; ответ `do_sync` ~10 мс при 3-сек заглушке.

## [2.0.2] (ARM) — 2026-06-20

ПАТЧ: вкладка настроек всё ещё падала с HTTP 500 после 2.0.1 — по второй,
независимой причине. DSM монтирует UI пакета как симлинк
`/usr/syno/synoman/webman/3rdparty/YandexDisk → /var/packages/YandexDisk/target/ui`,
и `settings.cgi` приходит через него. Путь к обёртке (`YD_BIN`) CGI вычислял от `$0`
логическим `pwd`, который НЕ разворачивает симлинк: `scripts/../..` уводило в
`…/webman/3rdparty`, и `YD_BIN` указывал на несуществующий файл → обёртка не
вызывалась → 500. (В 2.0.0 это маскировалось более ранним зависанием `sudo`,
устранённым в 2.0.1.) Диагностировано на NAS по `diag.cgi`/прямым прогонам обёртки.

### Fixed
- **`settings.cgi`: резолв `YD_BIN`/`SELF_DIR` через `pwd -P`** (физический путь) —
  разворачивает webman-симлинк, и `../..` корректно ведёт в `target/`. Плюс
  подстраховка: если резолв промахнётся — откат на `/usr/local/bin/yandex-disk`, затем
  на канонический `/var/packages/YandexDisk/target/yandex-disk`.

### Tests
- Герметичный **T45**: воспроизводит раскладку с webman-симлинком и зовёт `settings.cgi`
  через него БЕЗ `YD_BIN` — get-config обязан найти обёртку и вернуть конфиг. Прежние
  T39–T44 этот путь не покрывали (задают `YD_BIN` явно). Mutation-sanity: возврат `pwd`
  вместо `pwd -P` роняет T45 с тем же `HTTP 500`.

## [2.0.1] (ARM) — 2026-06-20

ПАТЧ: исправлена вкладка настроек на NAS. После установки 2.0.0 (и сброса кэша DSM)
новый интерфейс открывался, но падал с «Не удалось загрузить — HTTP 500», а
сохранение и «Синхронизировать сейчас» не работали. Причина выяснена диагностикой
`diag.cgi`: 3rdparty-CGI исполняется как САМ пользователь пакета `sc-yandexdisk`
(`conf/privilege: run-as: package`; euid 256139) — а НЕ как root, как ошибочно
заключил спайк Phase 0. При этом `settings.cgi` делегировал работу через
`sudo -u sc-yandexdisk`, но `sc-yandexdisk` беспарольно `sudo` не может, и в контексте
synoscgi `sudo` зависает → HTTP 500. Контракты, движок rclone, формат `sync.state` и
лог-маркеры не менялись.

### Fixed
- **`settings.cgi`: privilege-drop убран — все операции (`get-config`, `check-folder`,
  `set-folder`, `set-token`, `sync`) выполняются НАПРЯМУЮ.** CGI уже исполняется как
  `sc-yandexdisk`, поэтому `sudo` не нужен (а в synoscgi только ломает): чтения читают
  конфиг, записи создают `config.cfg`/`rclone.conf` сразу с владельцем `sc-yandexdisk`
  (`rclone.conf` `0600`, читается периодическим sync) — без root и без `chown`. Это чистая
  «ветка A» плана §3.1 в простейшем виде; привилегии пакета (`run-as: package`) не меняются.

### Tests
- Герметичный **T44** переписан: на `PATH` кладётся «отравленный» `sudo` (всегда падает);
  и чтение (`get-config`), и **запись** (`set-token`) через `settings.cgi` обязаны пройти —
  доказательство, что CGI не обращается к `sudo` ни на одном пути. Mutation-sanity: возврат
  `sudo` в делегирование роняет T44.
- off-NAS-гейт **`test/check-ui-contract.sh`**: дополнительно запрещает `sudo` в коде
  `settings.cgi` (CGI = пользователь пакета); плюс прежние проверки актуального дашборд-UI.

## [2.0.0] (ARM) — 2026-06-20

МАЖОР: первичная настройка пакета **полностью перенесена в интерфейс DSM** —
OAuth-токен, папка синхронизации и первый запуск теперь задаются в окне пакета,
**без SSH** (раньше это были шаги в терминале). Это меняет основной способ работы
с пакетом, поэтому версия мажорная. Обратная совместимость сохранена: уже
настроенные `config.cfg`/`rclone.conf` переживают обновление и видны в UI как
заданные ранее; SSH остаётся запасным/диагностическим путём. Движок rclone, формат
`sync.state` и лог-маркеры **не менялись**. План — `docs/ui-config-migration-plan.md`.

### Added
- **Настройка из UI (без SSH).** Новая раскладка-дашборд по образцу Synology Hyper
  Backup (мастер-детейл): слева список папок, справа шапка статуса и две карточки.
  Диалог «Добавить/Настроить папку» задаёт локальную папку, удалённый ресурс и
  чистку thumbs; секция «Подключение к Яндекс Диску» принимает вставленный
  JSON-токен (Tier 1); кнопка «Синхронизировать сейчас» запускает тот же `sync`,
  что и Планировщик задач; живая проверка папки показывает права `sc-yandexdisk`.
  Расписание остаётся ручной задачей Планировщика — UI даёт готовую к копированию
  команду (Опция A). Дизайн — `design/spec.md`, эталон раскладки — `design/example_1.png`.
- **Подкоманды обёртки `yandex-disk`** (контракт расширен обратносовместимо):
  `set-folder <dir> [remote] [clean_thumbs]` (атомарная запись `config.cfg`),
  `set-token` (JSON со stdin → `rclone.conf` `0600`), `get-config` (преднаполнение
  формы — **без значения токена**, только флаг «настроен»), `check-folder <dir>`
  (существование/права/владелец). Добавлены в `usage`.
- **Тонкий CGI `settings.cgi`** (эндпоинт `settings`): `GET` отдаёт состояние формы
  (JSON), `POST` делегирует запись и `sync` обёртке. Мутации требуют `SynoToken`
  (CSRF) в аутентифицированной админ-сессии DSM.
- **Helpers в `common.sh`** — единый источник истины валидации/записи:
  `validate_dir`/`validate_remote`/`validate_token`/`is_token_configured`/
  `write_cfg_atomic`; переиспользуются обёрткой и покрыты герметичными тестами.

### Security
- OAuth-токен **никогда** не передаётся через argv (не виден в `ps`) и не попадает
  в лог, stdout, историю или ответ CGI — наружу только флаг «Подключено». `rclone.conf`
  — `0600`, владелец `sc-yandexdisk`, запись атомарна (`umask 077`).
- CGI исполняется как root (3rdparty в этом DSM), поэтому конфиги пишутся **со
  сбросом привилегий** до `sc-yandexdisk` (`sudo -u sc-yandexdisk yandex-disk
  set-folder|set-token`) — файлы сразу `sc-yandexdisk`-овые, без root-owned секретов.
  Жёсткая валидация `dir`/`remote`/токена на стороне обёртки защищает root-CGI от
  инъекций. `run-as: package` не повышается, `--exclude`-список `_bisync` не урезан.

### Changed
- **Документация и описания — путь UI как основной.** `spk/INFO`
  (`description`/`description_rus`), `README.md` и `docs/terminal-setup.md`
  переписаны: настройка в интерфейсе — основной сценарий, SSH — запасной/
  диагностический. `RELEASE-INFO-ARM.txt` — версия, дата сборки, имя `.spk`,
  SHA-256 и размер под новый артефакт. Существующие настройки при обновлении не
  сбрасываются (`pre/postupgrade` уже переносят `home/.config`); старый `config.cfg`
  без новых ключей и 3-полевой `sync.state` остаются читаемыми; имя remote
  `yandexdisk`, ключи вкладок UI и формат статуса — без изменений.

## [1.2.3] (ARM) — 2026-06-13

ПАТЧ: исправление **воспроизводимости сборки** — `.spk` теперь собирается
байт-в-байт одинаково на любой машине (WSL и раннер GitHub Actions давали
разный SHA-256 при идентичном содержимом). Скрипты пакета, работающие на NAS,
**не изменены** — правка только в сборочной инфраструктуре и в концах строк
одного UI-файла; функционально пакет идентичен 1.2.2.

### Fixed
- **Недетерминизм порядка файлов в `.spk`.** Внешний tar в `build.sh` собирался
  без `--sort=name`, поэтому каталоги `conf/`/`scripts/` попадали в архив в
  порядке `readdir` файловой системы (на WSL и на ext4-раннере он разный) —
  две корректные сборки давали разный SHA-256. Добавлен `--sort=name`
  (внутренний `package.tgz` его уже имел); `INFO` остаётся первым в потоке
  (контракт DSM цел). `check-reproducible.sh` этот класс дефекта не ловил, т.к.
  две сборки на одной машине дают одинаковый `readdir`-порядок.
- **CRLF в упаковываемых текстовых файлах.** CRLF-гейт `build.sh` проверял
  только `POSIX_SH`-скрипты; `spk/package/ui/config` был закоммичен с CRLF, а
  `LICENSE`/`LICENSE.rclone`/`*.js`/`*.css` не были запинены в `.gitattributes`
  (на `core.autocrlf=true`-чекауте портились локально). Гейт расширен на **все**
  упаковываемые текстовые файлы (бинарники пропускаются); недостающие пути
  запинены как `eol=lf`; `spk/package/ui/config` нормализован к LF (содержимое
  идентично, изменены только концы строк).

### Changed
- **`RELEASE-INFO-ARM.txt`**: актуализированы `SHA-256 (.spk)` и размер под
  воспроизводимый артефакт (прежний размер тоже был устаревшим).

## [1.2.2] (ARM) — 2026-06-13

ПАТЧ: обновление движка `rclone` v1.74.2 → v1.74.3 (последний стабильный
релиз). Скрипты пакета, работающие на NAS, не изменены; контракт лог-маркеров
(счётчики `count_run_transfers` и авто-recovery) сохранён — все 6 маркеров
подтверждены в новом бинаре гейтом `test/check-rclone-contract.sh`.

### Changed
- **Движок `rclone` v1.74.2 → v1.74.3.** В `build.sh` обновлены URL и обе
  SHA-256 (zip — из официального `downloads.rclone.org/v1.74.3/SHA256SUMS`,
  бинарник — хэш из проверенного по этой сумме zip). Версия движка
  синхронизирована в `RELEASE-INFO-ARM.txt` и `README.md` (гейт
  `check-version-drift.sh`). Шим `test/fake-rclone` и golden-эталоны
  `status` (`test/golden/`) обновлены на `v1.74.3-fake`. Полный цикл гейтов
  (build/hermetic/coverage/reproducible/mutation) — зелёный.

## [1.2.1] (ARM) — 2026-06-13

Инфраструктура детерминизма (по `docs/deterministic-result-guideline*.md`):
герметичные поведенческие тесты без NAS, обязательные статические гейты
`build.sh`, самопроверка воспроизводимости, CI на всех ветках. Скрипты пакета,
работающие на NAS, не изменены; ПАТЧ — исправление сборочной инфраструктуры
(CRLF-дефект Д-7) и новые машинные проверки.

### Added
- **DET-1 — герметичный поведенческий тест `test/run-hermetic.sh` (Г-2 W2, W7).**
  Пакет целиком запускается во временном каталоге через `YD_*`-оверрайды, движок
  подменяется шимом `test/fake-rclone` (resync-aware, с фиксом дефекта Д-1 из Г-2).
  17 сценариев: машина состояний `run_bisync` (первый прогон/`--resync`, ошибка,
  авто-recover успешный и нет), окно счётчиков «только последний прогон»,
  single-run guard (flock), ротация лога, `is_configured`/`resolve_remote`,
  таблица входов парсера `cfg()`, контракт CLI-флагов `_bisync` со всеми
  6 exclude-масками (T13 — впервые машинно охраняет security-инвариант),
  7/3-полевой `sync.state` и golden-снимки вывода `status`/`sync_state_line`
  (`test/golden/`, нормализация путей и таймстемпов).
- **DET-2 — контракт-гейты**: `test/check-rclone-contract.sh` — 4 маркера
  счётчиков + 2 триггера recovery обязаны присутствовать в бинаре rclone
  (защита от слепого бампа движка); `test/check-version-drift.sh` — версия
  пакета `INFO`=`CHANGELOG`=`RELEASE-INFO`, версия движка `build.sh`=
  `RELEASE-INFO`=`README`; `test/check-reproducible.sh` — две чистые сборки
  обязаны дать один SHA256. Mutation-sanity: все 6 диверсий из Г-2 §5 прогнаны,
  каждая поймана своим гейтом.
- **DET-3 — `CLAUDE.md` (канон для AI-агентов, Г-2 §3)** и `.github/CODEOWNERS`
  на защищённый периметр гейтов (`build.sh`, `test/`, workflows, `CLAUDE.md`).
  Процесс работы с ИИ описан в `docs/ai-workflow.md`.

### Changed
- **DET-4 — `build.sh`: гейты тотальны (Д-3).** Отсутствие
  shellcheck/checkbashisms/python3 — ошибка, а не skip; `shellcheck -S warning`
  блокирующий; ELF-проверка архитектуры rclone — жёсткий отказ вместо WARN;
  новый CRLF-гейт по списку `POSIX_SH`; среда зафиксирована
  (`LC_ALL=C LANG=C TZ=UTC`, `umask 022`); `SOURCE_DATE_EPOCH` как канонический
  источник mtime (по умолчанию 0 — контрольные суммы прежних сборок не меняются).
- **DET-5 — CI `build-spk.yml` (Д-2).** Push-гейт на всех ветках (раньше — только
  `main`/`arm-support`); actions запинены по commit-SHA; перед сборкой —
  герметичные тесты, после — самопроверка воспроизводимости; ставится
  checkbashisms (devscripts).

### Fixed
- **DET-6 — CRLF-дрейф рабочего дерева (Д-7).** `spk/scripts/preupgrade` и
  `postupgrade` не были перечислены в `.gitattributes`: при `core.autocrlf=true`
  (WSL/Windows-чекаут) оба молча получали CRLF — `dash -n` падал с невнятной
  ошибкой, а собранный из такого дерева `.spk` унёс бы неисполняемые скрипты на
  NAS. Файлы добавлены в `.gitattributes` (`text eol=lf`); рецидив ловится новым
  CRLF-гейтом `build.sh`.

## [1.2.0] (ARM) — 2026-05-31

История синхронизации теперь показывает счётчики файлов за каждую синхронизацию с
явными именами **NAS** и **Yandex Disk** (отправлено/получено/изменено/удалено).
Минорная версия — новая, обратно совместимая функциональность.

### Added
- **HIST-1 — счётчики файлов в истории синхронизации.** Каждая запись на вкладке
  «История» (и строка «Последняя синхронизация» на вкладке «Статус») дополнена
  второй строкой с числом перенесённых файлов за прогон и **явными направлениями**:
  `Файлы — NAS → Yandex Disk (отправлено): N; Yandex Disk → NAS (получено): N;
  изменено: N; удалено: N`. Видно прямо в UI, без SSH, сколько и в какую сторону
  ушло за последнюю синхронизацию.

### Changed
- **HIST-2 — `var/sync.state` расширен счётчиками прогона.** Формат файла состояния
  дополнен полями `sent|received|modified|deleted` после прежних
  `status|timestamp|result` (теперь 7 полей вместо 3). Счётчики получает новая
  функция `count_run_transfers()` в `spk/package/yandex-disk`: она берёт хвост
  `var/logs/rclone.log` после последнего маркера `===== sync started` и считает
  строки `bisync` по точным подстрокам из движка — `Queue copy to Path2`
  (NAS → Yandex Disk, отправлено), `Queue copy to Path1` (Yandex Disk → NAS,
  получено), `Copied (replaced existing)` (изменено), `Queue delete` (удалено).
  Подстроки подтверждены в бинарнике rclone v1.74.2. Четыре прежние прямые записи
  `… > "$STATE_FILE"` заменены вызовом `write_sync_state`. На первом прогоне
  (`--resync`) `bisync` может не выводить строки `Queue copy to PathN`, поэтому
  счётчики там могут быть нулевыми — это допустимо.
- **HIST-3 — `sync_state_line()` печатает вторую строку.** Общий помощник в
  `spk/package/common.sh` разбирает новые поля и, если они присутствуют, добавляет
  отдельную строку со счётчиками и именами NAS / Yandex Disk. Полная обратная
  совместимость: старые 3-полевые файлы `sync.state` (без счётчиков) по-прежнему
  выводят только первую строку. Изменение затрагивает обе вкладки — «Статус» и
  «История» (логгер пишет многострочный `$CURRENT` в `status_history.log`, который
  `log.cgi` отдаёт как есть).
- `spk/INFO`: `version` `1.1.2` → `1.2.0`.

## [1.1.2] (ARM) — 2026-05-31

Документация: в `INFO:description_rus` (Центр пакетов) добавлены пошаговые
инструкции первоначальной настройки по SSH. Патч-версия — изменение только
метаданных/описания, без правок кода и поведения пакета.

### Changed
- **DESC-3 — пошаговая настройка по SSH в `INFO:description_rus`.** Описание
  пакета в Центре пакетов дополнено нумерованными шагами: права на папку
  синхронизации для `sc-yandexdisk`; запуск мастера `yandex-disk setup`; ответы
  мастера `rclone config` (remote `yandexdisk`, тип `yandex`, пустые
  `client_id`/`client_secret`, без авто-конфига/браузера); получение OAuth-токена
  командой `rclone authorize yandex` на ПК с браузером и вставка его в
  приглашение `config_token>`; заполнение `config.cfg` (`dir`, `remote`); первый
  `yandex-disk sync` (`rclone bisync --resync`) и настройка периодичности в
  Планировщике задач DSM. Строки разделены `\n` (переносы в Центре пакетов);
  во избежание поломки парсинга `INFO` в тексте нет ASCII-кавычек (используются
  «ёлочки»). Текст согласован с разделом README «Первоначальная настройка (по
  SSH)». Английское `description` оставлено без изменений.

## [1.1.1] (ARM) — 2026-05-31

Исправление логирования: `rclone.log` не пополнялся при штатной синхронизации.
Патч-версия — исправление ошибки без новой функциональности.

### Fixed
- **LOG-1 — `rclone.log` не пополнялся при успешной синхронизации.** `yandex-disk sync`
  запускал `rclone bisync` без `-v`, а на уровне по умолчанию (`NOTICE`) успешный
  (или «нечего синхронизировать») прогон не пишет в лог ничего: переносы файлов и
  итог `Bisync successful` относятся к уровню `INFO`. В лог попадали только ошибки,
  из‑за чего вкладка «Синхронизация» выглядела «застывшей». Добавлен флаг `-v` (INFO);
  объём ограничен прежней ротацией `rotate_log` (1 МБ + один бэкап `.1`).

### Changed
- **LOG-2 — маркеры прогонов в `rclone.log`.** Каждый запуск помечается строкой
  `===== sync started <время> =====`, а пропуск из‑за ещё идущего предыдущего прогона
  (flock) — строкой `===== sync skipped … (previous run still in progress) =====`.
  Лог всегда отражает свежую активность, даже когда rclone сказать нечего, а паузы
  объяснены явно, а не выглядят как «синхронизация молча остановилась».

## [1.1.0] (ARM) — 2026-05-31

Правки группы **P1** из QA/UX-аудита (`docs/qa-ux-audit.md`): сохранность данных при
обновлении, DSM-нативность метаданных, локализация и UX. Минорная версия — добавлена
новая, обратно совместимая функциональность.

### Added
- **VER-1 — защита данных при обновлении.** Добавлены `spk/scripts/preupgrade` и
  `postupgrade`: перед заменой `target/` они сохраняют OAuth-токен
  (`home/.config/rclone/rclone.conf`), `config.cfg`, метку базовой точки bisync
  (`var/.bisync_resynced`), `var/sync.state` и кэш листингов bisync в
  `$SYNOPKG_TEMP_UPGRADE_FOLDER`, а после обновления восстанавливают то, что не
  пережило замену (**restore-if-missing** — пережившие данные не затираются). `cp -a`
  сохраняет владельца (`sc-yandexdisk`) и права `0600`, поэтому запрещённый в DSM 7
  `chown` не нужен. Снимает риск повторной настройки по SSH при каждом обновлении.
  ⚙️ Проверку «обновление поверх» на самом DS124 (`docs/test.md` 10.4) ещё предстоит
  прогнать на железе.
- **DESC-1/2 — `INFO:description_rus`.** Русское описание для Центра пакетов с честными
  ожиданиями (ARM, движок rclone/MIT, настройка по SSH, нет резидентного демона —
  период в Планировщике, сторонний пакет, не аффилирован с Yandex). Английское
  `description` сокращено и выровнено по тем же тезисам.
- **UI: строка-подпись под вкладками** — поясняет назначение каждой вкладки.

### Changed
- **UX-2 — вкладки UI.** «Лог» → **«История»**, «Синхронизация» → **«Журнал
  синхронизации»** (внутренние ключи `status`/`log`/`sync_log` и CGI-эндпойнты не
  изменились). Кнопка **«Очистить»** теперь видна **постоянно** на вкладке «История»
  (в шапке), а не только при прокрутке вниз — убрана скролл-логика
  (`handleScroll`/`showClearButton`); `main.js` переписан на data-driven вкладки.
- **UX-3 — локализация `yandex-disk status`.** Русские ярлыки: «Движок синхронизации»,
  «Локальная папка», «Удалённый ресурс», «Последняя синхронизация» (диагностические
  токены `idle/ok`, `rc=N` сохранены). Затрагивает и историю на вкладке «История».
- **LIC-2(4)/LIC-3/CON-1 — атрибуция.** Добавлен файл `NOTICE`: происхождение форка
  (исходный автор VitalityV1nT; у апстрима лицензия **не указана** — проверено
  2026-05-31), распространитель volkovpv, точки поддержки, rclone (MIT), товарные знаки
  Yandex/Synology. В `README.md` добавлен раздел «Происхождение, лицензия и права».
- `spk/INFO`: `version` `1.0.1` → `1.1.0`.
- `build.sh`: `preupgrade`/`postupgrade` включены в статические проверки и помечаются
  исполняемыми.
- `docs/test.md` приведён в соответствие (ярлыки `status`, имена вкладок, постоянная
  кнопка «Очистить», сценарий обновления 10.4).

## [1.0.1] (ARM) — 2026-05-31

Правки группы **P0** из QA/UX-аудита (`docs/qa-ux-audit.md`): корректность прав,
лицензии, метаданных и инструкций. Функционально пакет не меняется — это PATCH.

### Added
- **`spk/LICENSE.rclone`** — официальный MIT-текст rclone (Copyright (C) 2012
  Nick Craig-Wood). Вкладывается в `.spk` рядом с бинарником rclone, выполняя
  требование MIT сохранять уведомление о лицензии при распространении. *(LIC-2(2))*
- **`spk/INFO`**: добавлены `thirdparty="yes"`, `distributor="volkovpv"`,
  `distributor_url`, `support_url` (issues текущего репозитория) — пакет честно
  помечен как сторонний, точки поддержки разведены с исходным автором. *(DSM-2)*

### Changed
- **`spk/LICENSE` переписан.** Раньше DSM при установке показывал (с обязательным
  «Я согласен») устаревшую x86-инструкцию по установке — то есть пользователь
  соглашался с инструкцией к другому продукту. Теперь это реальные условия:
  лицензия кода пакета (**MIT**), уведомление о стороннем `rclone` (MIT, ссылка на
  `LICENSE.rclone`), дисклеймер о товарных знаках Yandex/Synology и об отсутствии
  аффилиации, отказ от гарантий. *(LIC-1)*
- **README переустроен.** `README-ARM.md` стал главным `README.md` (актуальная
  ARM/rclone-инструкция теперь — первая страница репозитория); прежний x86-README
  перенесён в `docs/README-x86-legacy.md` с пометкой «архив». Из ARM-README убрана
  перекрёстная ссылка на x86-«общие шаги»; устаревшая ручная инструкция сборки
  (`tar … YandexDisk.spk`) осталась только в архиве. *(DOC-1 / DOC-2 / DOC-4)*
- **`spk/INFO`**: `maintainer` `VitalityV1nT` → `volkovpv` (текущий распространитель
  ARM-сборки); исходный автор кредитуется в `LICENSE` и `README`. *(DSM-2 / LIC-3)*
- **`build.sh`** вкладывает `LICENSE.rclone` в `.spk` (рядом с `LICENSE`).
- **`build_notes.txt` переписан** под реальность сборки: движок — `rclone` v1.74.2
  (докачка `build.sh` по SHA-256), а не ARM64 `.deb` клиента Яндекса (его для ARM
  не существует). *(CON-2)*
- `spk/INFO`: `version` `1.0.0` → `1.0.1`.

## [1.0.0] (ARM) — 2026-05-30

Первая стабильная версия ARM-сборки. Счётчик версий **сброшен до `1.0.0`**:
ARM-порт (движок rclone, aarch64, своя модель синхронизации) достаточно разошёлся
с базовым x86-пакетом, чтобы вести собственную линию версий, отвязанную от
`0.1.6.1080`.

### Changed
- **Введено семантическое версионирование** `МАЖОР.МИНОР.ПАТЧ` с обнулением
  младших разрядов и без DSM-суффикса сборки (`-N`); правила и порядок выбора
  версии — в разделе «Версионирование» выше. Версию определяет ИИ, выполняющий
  задачу.
- `spk/INFO`: `version` `0.1.6.1080-15` → `1.0.0`. Имя артефакта теперь
  `YandexDisk-ARM-1.0.0.spk`. Функционально пакет идентичен `0.1.6.1080-15` —
  меняется только строка версии. Апгрейд с прежней версии DSM распознаёт штатно
  (`1` > `0` в старшем разряде; пакет уже принимал 4-сегментные версии).

## [0.1.6.1080-15] (ARM) — 2026-05-30

Сборка и релиз-процесс: версия в имени артефакта и автоматизация GitHub.

### Changed
- **Имя `.spk` содержит версию.** `build.sh` подставляет версию из `spk/INFO` в имя
  файла — `YandexDisk-ARM-<версия>.spk` (напр. `YandexDisk-ARM-0.1.6.1080-15.spk`)
  вместо безымянного `YandexDisk-ARM.spk`. Ассеты GitHub Release теперь
  самоописательны, а ревизии не перезаписывают друг друга. Версия читается один раз
  в начале сборки (убрано дублирующее чтение `spk/INFO`).

### Added
- **CI/Release через GitHub Actions** (`.github/workflows/build-spk.yml`). На каждый
  push/PR (`main`/`arm-support`) — сборка `.spk` + статические проверки, результат
  доступен как workflow artifact. На тег `v*` — сборка и **публикация** версионного
  `.spk` + `.sha256` как ассетов GitHub Release (через `gh`, без сторонних actions;
  `permissions: contents: write`). Runner `ubuntu-latest` уже содержит
  `unzip`/`curl`/`shellcheck`/`python3`, поэтому `build.sh` отрабатывает «как есть»
  (докачивает rclone по контрольной сумме).
- **Документация по сборке в `README-ARM.md`** — раздел «🛠 Сборка пакета»:
  локальная сборка (`./build.sh`, зависимости, шаги, проверка SHA-256) и
  сборка/релиз через GitHub Actions (тег → Release). Зафиксирован `eol=lf` для
  `*.yml`/`*.yaml`.

## [0.1.6.1080-14] (ARM) — 2026-05-30

Набор «P1» из аудита: снижение фоновой нагрузки на DS124 (1 ГБ RAM),
диагностируемость синхронизации в UI и сокращение технического долга после
миграции с x86-демона на rclone.

### Removed
- **C2 — ежечасный `yandex-cleaner` удалён.** Постоянный фоновый процесс раз в час
  рекурсивно обходил всю папку синхронизации и делал `rm -rf @eaDir` + удалял
  `Thumbs.db`. Это было и бесполезно (`bisync` и так исключает `@eaDir/**` и
  `Thumbs.db` — наверх они не уходят), и вредно: удаление `@eaDir` воюет с
  медиа-индексатором Synology, который тут же пересоздаёт их (бесконечная
  churn-петля + потеря миниатюр). Опциональная локальная чистка теперь —
  **один шаг внутри `sync`** (`clean_thumbs=1` в `config.cfg`, по умолчанию выкл.),
  и удаляет только `Thumbs.db`/`.DS_Store`, **не трогая** `@eaDir`.
- **D2 — мёртвый код.** Удалён неиспользуемый эндпойнт `last_status.cgi` (UI ходит
  только в `status.cgi`/`log.cgi`/`sync_log.cgi`) и связанный с ним `last_status.log`.

### Added
- **E1 — лог синхронизации виден в UI.** Новая вкладка **«Синхронизация»**
  (`sync_log.cgi`) показывает последние 300 строк `var/logs/rclone.log` — при
  `rc=N`/конфликте причина видна прямо в интерфейсе, без SSH. Раньше реальный вывод
  bisync нигде в UI не отображался (только `error|время|rc=N` в истории статусов).
- **F1 — бинарники убраны из git.** `spk/package/rclone` (72 МБ) и
  `YandexDisk-ARM.spk` (24 МБ) больше не отслеживаются — раздаются как
  **GitHub Release assets**. `build.sh` воспроизводит `rclone` из официального
  релиза по контрольной сумме (сверка SHA256 и zip, и распакованного бинарника) и
  пересобирает `.spk`. Зип кэшируется локально в `downloads/` (тоже вне git).
  Сборка `.spk` сделана **воспроизводимой байт-в-байт** (фиксированные mtime +
  `gzip -n`), поэтому SHA-256 в `RELEASE-INFO-ARM.txt` повторяется при чистой
  пересборке, а не меняется от прогона к прогону.

### Changed
- **A4/A5/D2 — `yandex-logger` переписан под новую модель.** Был резидентный
  bash-цикл, который раз в 60 c звал `yandex-disk status` (а тот — `rclone`), вызывал
  обёртку по `PATH` (мог получить пустой статус из systemd-юнита) и содержал мёртвую
  ветку «Error: daemon not started» от x86-демона. Теперь это лёгкий **heartbeat на
  POSIX sh**: читает `var/sync.state` напрямую (без спавна `rclone`/обёртки), пишет
  историю по изменениям состояния, интервал 5 мин. Назначение прежнее — держать
  пакет «Запущено» на DSM 7 (по живому процессу `start-stop-status status`
  возвращает `0`); удешевлён, но не удалён.
- **C3 — ротация логов.** `rclone.log` (пишет `sync`) и `status_history.log` (пишет
  heartbeat) ротируются по размеру (>1 МБ → `.1`, одна резервная копия) — раньше
  росли без ограничения.
- **D1 — общий `common.sh`.** Пути пакета, парсер `config.cfg` (`cfg()`), ротация
  логов (`rotate_log`) и сводка `sync.state` (`sync_state_line`) вынесены в
  `target/common.sh` и подключаются (`.`) обёрткой, `start-stop-status` и логгером.
  Раньше путь `/var/packages/YandexDisk` и парсер `dir` дублировались по нескольким
  файлам (приходилось править в двух местах). Переменная `DAEMON_LOG` (наследие
  демона) переименована в осмысленную `RCLONE_LOG`.
- **D3/D4 — чистка скриптов.** `start-stop-status stop` теперь матчит
  `/scripts/yandex-logger`, а не подстроку `yandex-logger` в любой команде.
  Все скрипты пакета приведены к **POSIX sh** (bash-зависимость ушла вместе с
  `yandex-cleaner`); `build.sh` проверяет их единым `dash -n` + `shellcheck`
  (линт чистый).

## [0.1.6.1080-13] (ARM) — 2026-05-30

Набор «P0» из аудита: корректность синхронизации, диагностируемость и снижение
накладных расходов на целевом железе (DS124, 1 ГБ RAM).

### Fixed
- **A1 — конфликт-резолюция приведена в соответствие с README.** `rclone bisync`
  запускается с `--conflict-resolve path1`: при двустороннем изменении одного файла
  побеждает **локальная** копия (как и обещала документация). Выбор `path1`, а не
  `newer`, намеренный — бэкенд Yandex не хранит mtime точно (см. `-11`), поэтому
  решение «по новизне» было бы недетерминированным. Конфликтная версия с Диска не
  теряется: сохраняется рядом как переименованная копия. Раньше bisync шёл без
  `--conflict-resolve` (дефолт `none`) и плодил дубликаты `..path1/..path2`.
- **A3 — лог реальной синхронизации сохраняется.** `_bisync` получил
  `--log-file var/logs/rclone.log --log-format date,time`. Раньше переменная
  `DAEMON_LOG` была объявлена, но не использовалась, и причина сбоя bisync не была
  видна ни в UI, ни в файле (только `rc=N`). Само-восстановление базы (`--resync`)
  теперь распознаёт триггер «cannot find prior / Must run --resync» и в лог-файле,
  и в перехваченном stderr.

### Added
- **A2 — защита от перекрытия запусков (`flock`).** `yandex-disk sync` берёт
  неблокирующий `flock` на `var/sync.lock`: если предыдущий прогон (например,
  долгий первичный `--resync`) ещё идёт, повторный запуск из Планировщика тихо
  завершается («sync already running, skip») и не конкурирует за кэш bisync.

### Changed
- **C1 — версия rclone кэшируется.** `status` больше не спавнит 72 МБ-бинарник
  rclone на каждый вызов (логгер опрашивал статус раз в минуту → ~1440 запусков
  в сутки только ради неизменной строки версии). Версия читается из
  `var/rclone.version`; `start-stop-status` сбрасывает кэш при (пере)старте пакета,
  чтобы подхватить обновление rclone.
- **G1 — `README-ARM.md` вычищен от наследия резидентного демона.** Убраны
  противоречивые упоминания «резидентного демона» (установка, Шаг 5, раздел
  расписания) и неиспользуемый параметр `interval` из примера `config.cfg`. Единая
  модель — «Планировщик задач DSM». Добавлено указание на лог `var/logs/rclone.log`.

## [0.1.6.1080-12] (ARM) — 2026-05-29

### Changed
- **Убран резидентный sync-демон.** На DSM 7 фоновый shell-цикл ненадёжен под
  systemd (его прибивает, остаются bisync-блокировки, пакет уходит в «остановлен»).
  Теперь периодическая синхронизация настраивается как задача **Планировщика задач
  DSM**, запускающая `yandex-disk sync`.
  - `start-stop-status` больше не запускает bisync-цикл; стартует только лёгкие
    `yandex-logger`/`yandex-cleaner` (статус в UI + пакет показывается «запущен»),
    статус определяется по `yandex-logger`.
  - Обёртка `yandex-disk`: убраны `start/stop/__daemon`; остались
    `sync | status | setup | version | rclone`. `sync` сам пишет `sync.state`
    (его читает `status`/UI), несёт `--inplace`, `--max-lock`, `--recover`,
    `--check-sync=false` и авто-`--resync` при потере базы.
  - `interval=` в `config.cfg` больше не используется (период задаётся в Планировщике).

## [0.1.6.1080-11] (ARM) — 2026-05-29

### Fixed
- **`--check-sync=false`**: бэкенд Yandex в rclone не сохраняет mtime точно, из-за чего
  строгая взаимная сверка bisync падала с «path1 and path2 are out of sync / Modtime not
  equal in listing» даже для идентичных по содержимому файлов (напр. local `2014` vs
  remote `2026`). Теперь расхождение mtime во взаимной сверке допускается.

## [0.1.6.1080-10] (ARM) — 2026-05-29

### Fixed / Changed
- **`--inplace`**: rclone пишет сразу в целевой файл, без временного `.partial`.
  Устраняет ошибку `chtimes …partial: no such file or directory`, которую вызывали
  фоновые службы Synology (индексация/антивирус), удаляя временные файлы.
- **`--max-lock 15m` + `--recover`**: авто-снятие зависших lock-файлов и восстановление
  после прерванных запусков (раньше «prior lock file found» блокировал синхронизацию).
- **Само-восстановление базы bisync**: при потере списков (`cannot find prior … /
  Must run --resync`) враппер сам выполняет `--resync` (раньше нужно было вручную).
- **`export HOME`** в обёртке — единый кэш bisync независимо от способа запуска
  (демон / Планировщик задач / вручную), иначе bisync терял состояние.
- **`--transfers 2 --checkers 4`** — щадящий режим для 1 ГБ ОЗУ DS124.

### Важно (настройка DSM)
- Надёжное периодическое расписание на DSM 7 — через **Планировщик задач** DSM
  (`yandex-disk sync` каждые N минут), т.к. резидентный демон под systemd ненадёжен.
  Подробности — в `README-ARM.md`, раздел «Расписание синхронизации».
- Папку синхронизации стоит **исключить из Индексирования и Антивируса** Synology.

## [0.1.6.1080-9] (ARM) — 2026-05-29

### Fixed
- **Пакет не запускался на DSM 7 (`synopkg start` → код 263 «failed to get unit
  status»).** Причина: в `conf/privilege` была указана `groupname: "sc-yandexdisk"`,
  но такой группы в системе нет — служебный пользователь `sc-yandexdisk` состоит в
  группе `synocommunity`. systemd-юнит пакета не мог разрешить несуществующую группу
  в `Group=` и падал. Возврат `groupname: "synocommunity"` (существующая группа,
  в которой и так состоит пользователь) чинит запуск.

## [0.1.6.1080-8] (ARM) — 2026-05-29

### Added
- **Поддержка ARM64 / aarch64** (Synology DS124, RTD1619B и другие aarch64-модели).
- Нативный движок **rclone v1.74.2** (`linux-arm64`, статически слинкован) с
  встроенным бэкендом Yandex Disk — вместо проприетарного `yandex-disk` CLI,
  который не имеет ARM-сборки.
- Обёртка `yandex-disk` (POSIX sh): `start|stop|status|sync|setup|version|rclone`,
  реализованная поверх `rclone bisync` (двусторонняя синхронизация). Сохраняет
  совместимость с UI пакета и скриптами `yandex-logger` / `yandex-cleaner`
  (в т.ч. строку `Error: daemon not started` для статуса).
- `rclone` добавлен в `conf/resource` (`usr-local-linker`) → доступен как
  `/usr/local/bin/rclone`.
- Документация: `README-ARM.md`, `RELEASE-INFO-ARM.txt`, `build_notes.txt`.
- Скрипты ручной проверки на NAS: `test-on-nas-install.sh`,
  `test-on-nas-functional.sh`.
- `.gitattributes` — фиксирует `eol=lf` для shell-скриптов (защита от
  CRLF при `core.autocrlf=true`).
- Контрольная сумма `YandexDisk-ARM.spk.sha256`.

### Changed
- `INFO`:
  - `displayname`: `Yandex Disk` → `Yandex Disk (ARM)`
  - `version`: `0.1.6.1080-3` → `0.1.6.1080-5` (числовая ревизия — требование DSM; «ARM» вынесено в `displayname`)
  - `conf/privilege` `groupname`: `synocommunity` → `sc-yandexdisk` (самодостаточная группа: на «чистом» NAS группы `synocommunity` нет, из-за чего DSM не создавал служебного пользователя).
  - `arch`: список x86 (apollolake, broadwell, …) → `rtd1619b rtd1296 armada37xx armada38x alpine4k`
  - `description`: дополнено про ARM64 / DS124 / rclone.
  - Поля `package`, `os_min_ver`, `maintainer`, `dsmuidir`, `dsmappname` — без изменений.
- `spk/package/yandex-disk`: бинарник x86_64 заменён обёрткой над rclone
  (оригинал сохранён как `spk/package/yandex-disk.x86_64.backup`, в `.spk` не входит).

### Fixed
- Нормализованы окончания строк **CRLF → LF** во всех shell-скриптах
  (`start-stop-status`, `yandex-logger`, `yandex-cleaner`, `ui/scripts/*.cgi`).
  CRLF (появлявшийся из-за `core.autocrlf=true`) ломал бы `#!/bin/sh` и
  разбор `case/if` при запуске на DSM.
- **Исправлена ошибка установки «Неверный формат файла»** в DSM:
  - `INFO` и `conf/*` нормализованы в LF (CRLF ломал разбор управляющих файлов);
  - версия приведена к числовой ревизии (`-arm`-суффикс отвергался валидатором DSM);
  - `.spk` пересобран с владельцем `root` и без PAX-заголовков (формат GNU tar).
- **Исправлена остановка пакета сразу после запуска** («остановлено вручную»):
  демон `yandex-disk` сделан **резидентным** — `start` всегда запускает фоновый
  процесс, который ждёт настройки (статус «waiting for setup»), а не завершается
  с ошибкой. Теперь DSM показывает «Запущено», а синхронизация стартует
  автоматически в течение ~30 c после настройки OAuth + папки.
- **Исправлено отсутствие пользователя `sc-yandexdisk`**: в DSM 7 пользователь
  создаётся самим DSM из `conf/privilege` (команды `synouser`/`synogroup` в
  скриптах пакета запрещены). Причиной была несуществующая группа `synocommunity`
  — заменена на самодостаточную `sc-yandexdisk`.
- **Пакет сам создаёт `~/.config/yandex-disk/config.cfg`** (каталог + шаблон с
  пустым `dir`) при `start`/`setup`, чтобы не возникала ошибка `E212` при ручном
  создании файла редактором.
- **Парсер `config.cfg` игнорирует инлайн-комментарии и пробелы** (`dir="…"  # …`):
  раньше текст после `#` попадал в значение → rclone падал с `rc=7`. Исправлено в
  обёртке `yandex-disk` и в `yandex-cleaner`; из примера в README убраны инлайн-комментарии.
- **Исключены системные папки Synology из синхронизации** (`#recycle`, `@eaDir`,
  `#snapshot`, `@tmp`, `.DS_Store`, `Thumbs.db`): раньше `rclone` спотыкался на
  `/volume1/<share>/#recycle` с `permission denied` и прерывал bisync.
- **Авто-подбор remote**: если указанного в `config.cfg` имени remote нет в
  `rclone.conf`, а настроен ровно один remote — используется он (терпит, когда
  remote назван иначе, напр. `Yandex` вместо `yandexdisk`).
- **Защита `interval`**: нечисловое значение откатывается к `300` (раньше мусор
  ломал `sleep` в цикле демона).

### Notes
- 32-битные платформы `armada38x` / `alpine4k` присутствуют в `arch` для широты
  установки, но нативный arm64-бинарник на них не запустится — нужна отдельная
  armv7-сборка rclone.
- Функциональная синхронизация с реальным аккаунтом Yandex Disk проверяется на
  NAS (скрипты `test-on-nas-*.sh`); на этапе сборки проверяются структура
  пакета, архитектура бинарника и корректность скриптов.
