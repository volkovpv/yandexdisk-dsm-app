# Тест-план: Yandex Disk (ARM) для Synology DSM

> Аудитория: разработчики/QA (Р).
> Статус: **исторический снимок** (составлен для версии пакета 1.1.0 / `rclone` v1.74.2;
> актуальная версия — см. `spk/INFO`). Сценарии ручной проверки на NAS остаются
> применимыми; версии бинарника/ОС в шапке — на момент составления. Документ
> заморожен как тест-план релиза и не синхронизируется с версионным гейтом.

**Тестируемый продукт:** пакет `YandexDisk-ARM-1.1.0.spk` (движок `rclone` v1.74.2, обёртка `yandex-disk`).
**Целевое устройство:** Synology **DS124** (Realtek RTD1619B, aarch64, 1 ГБ RAM).
**ОС:** DiskStation Manager **7.3.2-86009**.
**Автор пакета:** volkovpv (на основе работы VitalityV1nT).
**Составитель тест-плана:** команда «Senior DSM-разработчик + Senior QA».

> Из объёма проверки **исключены** проверка документации и сборки проекта (`build.sh`, CI,
> контрольные суммы, README). Покрываются: **установка, работоспособность, синхронизация, логи,
> UI, интеграция с Планировщиком и жизненный цикл пакета**.

---

## 1. Как пользоваться этим планом

### 1.1. Типы проверок («ящики»)
| Метка | Тип | Что значит для исполнителя |
|---|---|---|
| ⬛ **Ч** | Чёрный ящик | Только GUI DSM и документированные команды. Внутрь файлов не заглядываем. |
| 🟫 **С** | Серый ящик | Документированные команды **плюс** осмотр известных файлов/процессов/состояния. |
| ⬜ **Б** | Белый ящик | Целевая проверка внутренней логики и веток кода (flock, кэш версии, ротация, авто-recover, парсер конфига, коды возврата). |

### 1.2. Правила прогона
- **Тесты выполняются строго по порядку.** Состояние переносится между тестами: настроенный
  remote и базовая точка синхронизации создаются **один раз** (Разделы 3–4) и далее
  переиспользуются — повторно настройку не делаем.
- Каждый тест: **PASS**, если фактический результат совпал с ожидаемым; иначе **FAIL** (зафиксировать вывод).
- Команды выполняются **по SSH** на самом NAS. Многие — от имени пользователя пакета через `sudo -u sc-yandexdisk`;
  `sudo` спросит пароль **вашей учётки администратора DSM** (не пароль `sc-yandexdisk`).
- ⏱ — длительный тест (ожидание расписания/таймера). 🌐 — нужен компьютер с браузером и установленным `rclone`.

### 1.3. Предусловия стенда (выполнить один раз до начала)
1. DS124 на DSM 7.3.2, включён **SSH** (Панель управления → Терминал и SNMP).
2. Учётная запись администратора DSM; на ПК с браузером установлен `rclone` (для OAuth).
3. Действующий аккаунт Яндекс с Яндекс Диском (можно тестовый).
4. Создана общая папка для синхронизации, например `/volume1/yandexdisk`.
5. Файл `YandexDisk-ARM-1.1.0.spk` доступен для загрузки в Центр пакетов.

### 1.4. Переменные окружения для SSH-сессии
Вставьте один раз после входа по SSH — далее шаги используют эти сокращения:
```sh
PKG=/var/packages/YandexDisk
YD=/usr/local/bin/yandex-disk
RCLONE=/usr/local/bin/rclone
U=sc-yandexdisk
CFG=$PKG/home/.config/yandex-disk/config.cfg
RCONF=$PKG/home/.config/rclone/rclone.conf
LOGD=$PKG/var/logs
STATE=$PKG/var/sync.state
```

### 1.5. Справочник путей и процессов (для 🟫 С и ⬜ Б проверок)
| Объект | Путь / признак |
|---|---|
| Обёртка / движок | `$PKG/target/yandex-disk`, `$PKG/target/rclone` (симлинки в `/usr/local/bin/`) |
| Общий код | `$PKG/target/common.sh` |
| Heartbeat / статус-скрипт | `$PKG/scripts/yandex-logger`, `$PKG/scripts/start-stop-status` |
| Скрипты обновления (бэкап данных) | `$PKG/scripts/preupgrade`, `$PKG/scripts/postupgrade` |
| Конфиг приложения | `$CFG` (ключи `dir`, `remote`, `clean_thumbs`) |
| Конфиг remote (токен) | `$RCONF` |
| Состояние синхронизации | `$STATE` (формат `статус\|время\|результат`) |
| История статусов (вкладка «История») | `$LOGD/status_history.log` |
| Реальный лог bisync (вкладка «Журнал синхронизации») | `$LOGD/rclone.log` |
| Метка базовой точки | `$PKG/var/.bisync_resynced` |
| Кэш версии rclone | `$PKG/var/rclone.version` |
| Блокировка одиночного запуска | `$PKG/var/sync.lock` |
| Системный пользователь пакета | `sc-yandexdisk` (группа `synocommunity`) |
| CGI-эндпойнты UI | `/webman/3rdparty/YandexDisk/scripts/{status,log,sync_log,clear_log}.cgi` |

---

## 2. РАЗДЕЛ 1 — Установка пакета

| # | Тип | Шаги | Ожидаемый результат |
|---|---|---|---|
| **1.1** | ⬛ Ч | Центр пакетов → **Установка вручную** → выбрать `YandexDisk-ARM-1.1.0.spk` → подтвердить предупреждение об «неизвестном издателе» → установить. | Установка завершается без ошибок. В списке появляется **Yandex Disk (ARM)** со статусом **«Запущено»**. |
| **1.2** | 🟫 С | `id $U` | Пользователь `sc-yandexdisk` существует, входит в группу `synocommunity`. (DSM создаёт его сам — `synouser` в пакете не используется.) |
| **1.3** | 🟫 С | `ls -l $YD $RCLONE` | Оба — симлинки на `$PKG/target/...`, исполняемые. |
| **1.4** | ⬜ Б | `file $RCLONE` (или `od -An -tu1 -j18 -N1 $RCLONE`); затем `$RCLONE version` | `file` сообщает **ELF 64-bit … aarch64** (`e_machine = 183`). `rclone version` печатает версию **v1.74.2** и `os/arch: linux/arm64`. |
| **1.5** | 🟫 С | `ls -la $PKG/home/.config/yandex-disk/; cat $CFG` | Каталог конфигурации создан. `config.cfg` создан автоматически и содержит `dir=""` и `remote="yandexdisk:"` (поле `dir` пустое). |
| **1.6** | ⬛ Ч | Открыть главное меню DSM. | Присутствует иконка **Yandex Disk**; приложение запускается по клику (приложение не «для всех пользователей» — видно только администратору). |

---

## 3. РАЗДЕЛ 2 — Состояние «из коробки» (до настройки)

**Предусловие:** Раздел 1 пройден; remote ещё **не** настроен, `dir` пуст.

| # | Тип | Шаги | Ожидаемый результат |
|---|---|---|---|
| **2.1** | 🟫 С | `ps -eo cmd \| grep '[y]andex-logger'` | Процесс heartbeat `yandex-logger` запущен (ровно один). Именно он держит статус пакета «Запущено» — резидентного sync-демона в сборке нет. |
| **2.2** | 🟫 С | `ls -l $LOGD` | Каталог логов создан, есть пустой `status_history.log`. |
| **2.3** | 🟫 С | `sudo -u $U $YD status` | Вывод без падения, 4 строки: `Движок синхронизации: rclone …`, `Локальная папка: '<не задана>'`, `Удалённый ресурс: 'yandexdisk:'`, `Последняя синхронизация: ещё не выполнялась …`. Код возврата `0`. |
| **2.4** | ⬜ Б | `sudo -u $U $YD sync; echo "rc=$?"` | Печатает `Error: not configured yet — run: yandex-disk setup`, **rc=1** (нет `rclone.conf` и/или пуст `dir` → `is_configured` = false). |
| **2.5** | ⬛ Ч | Открыть приложение → вкладка **«Статус»**. | Видны 3 вкладки: **Статус / История / Журнал синхронизации**. На «Статус» — текущая дата и тот же вывод, что в 2.3. |
| **2.6** | ⬛ Ч | Вкладка **«Журнал синхронизации»**. | Текст: `Лог синхронизации пуст (rclone bisync ещё не запускался).` |
| **2.7** | ⬛ Ч | Вкладка **«История»**. | Пусто или только заголовок — история ещё не накоплена (sync не запускался). |

---

## 4. РАЗДЕЛ 3 — Первоначальная настройка OAuth и папки

**Предусловие:** Раздел 2 пройден. Выполняется **один раз**; результат используется во всех последующих разделах.

| # | Тип | Шаги | Ожидаемый результат |
|---|---|---|---|
| **3.1** | 🟫 С | `sudo -u $U $YD setup` (на вопрос интерактивного запуска ответить `N`). | Печатается пошаговая инструкция (OAuth, папка, Планировщик). Код возврата `0`. |
| **3.2** | 🟫 С 🌐 | На ПК: `rclone authorize "yandex"` → войти в Яндекс в браузере, скопировать JSON-токен. На NAS: `sudo -u $U $RCLONE config create yandexdisk yandex token '<JSON-ТОКЕН>' --config $RCONF` | `rclone.conf` создан и содержит секцию `[yandexdisk]`. Проверить права: `ls -l $RCONF` → файл `-rw-------` (0600). |
| **3.3** | 🟫 С | `sudo -u $U tee $CFG >/dev/null <<'EOF'`<br>`dir="/volume1/yandexdisk"`<br>`remote="yandexdisk:"`<br>`EOF` | `config.cfg` содержит реальный путь в `dir`. Путь существует, владелец/права позволяют `sc-yandexdisk` чтение-запись (проверить `sudo -u $U test -w /volume1/yandexdisk && echo OK`). |
| **3.4** | 🟫 С | `sudo -u $U $YD status` | Теперь строка `Локальная папка: '/volume1/yandexdisk'`, `Удалённый ресурс: 'yandexdisk:'`. `Последняя синхронизация` пока `ещё не выполнялась`. |
| **3.5** | 🟫 С | Права на папку в GUI: Панель управления → Общая папка → `yandexdisk` → Изменить → Разрешения → «Системный внутренний пользователь» → `sc-yandexdisk` → чтение/запись. | Права сохраняются; `sudo -u $U touch /volume1/yandexdisk/.probe && sudo -u $U rm /volume1/yandexdisk/.probe` выполняется без ошибок. |

---

## 5. РАЗДЕЛ 4 — Первая синхронизация и базовая точка

**Предусловие:** Раздел 3 пройден. На локальной папке и на Диске лежит то, что допустимо объединить (первый прогон делает `--resync`).

| # | Тип | Шаги | Ожидаемый результат |
|---|---|---|---|
| **4.1** | ⬜ Б | `sudo -u $U $YD sync; echo "rc=$?"` | Первый прогон выполняет `rclone bisync --resync`. По завершении **rc=0**; создаётся метка `$PKG/var/.bisync_resynced`; `cat $STATE` = `idle\|<дата-время>\|ok`. |
| **4.2** | 🟫 С | `ls -l $LOGD/rclone.log; tail -n 20 $LOGD/rclone.log` | Файл `rclone.log` создан, содержит вывод bisync; строки начинаются с даты и времени (формат `--log-format date,time`). |
| **4.3** | 🟫 С | `sudo -u $U $YD status` | Строка `Последняя синхронизация: <время> (idle/ok)`. |
| **4.4** | ⬜ Б | `cat $PKG/var/rclone.version` | Кэш версии создан (одна строка с версией rclone). |
| **4.5** | ⬜ Б | `echo 'rclone vTEST-CACHE' \| sudo -u $U tee $PKG/var/rclone.version >/dev/null` → `sudo -u $U $YD status \| head -1` → восстановить кэш: `sudo -u $U rm -f $PKG/var/rclone.version; sudo -u $U $YD status >/dev/null` | Первая строка status = `Движок синхронизации: rclone vTEST-CACHE (Яндекс Диск через rclone)`. Это доказывает, что `status` **читает кэш**, а не спавнит 72-МБ бинарник rclone на каждый вызов. После восстановления кэш заполняется реальной версией. |

---

## 6. РАЗДЕЛ 5 — Функциональная двусторонняя синхронизация

**Предусловие:** Раздел 4 пройден (есть базовая точка). Между шагами расписание Планировщика лучше отключить, чтобы синхронизацией управлять вручную.
**Соглашение:** проверка наличия файла на Диске — `sudo -u $U $RCLONE --config $RCONF lsf yandexdisk: | grep <имя>`.

| # | Тип | Шаги | Ожидаемый результат |
|---|---|---|---|
| **5.1** | 🟫 С | Создать локально: `sudo -u $U sh -c 'echo up > /volume1/yandexdisk/up.txt'` → `sudo -u $U $YD sync` → проверить на Диске. | Файл `up.txt` появился на Яндекс Диске (синхронизация **вверх**). `$STATE` = `idle\|…\|ok`. |
| **5.2** | ⬛ Ч | На Диске (веб-интерфейс Яндекса) создать файл `down.txt` → `sudo -u $U $YD sync` → `ls /volume1/yandexdisk/`. | Файл `down.txt` появился в локальной папке (синхронизация **вниз**). |
| **5.3** | 🟫 С | `sudo -u $U rm /volume1/yandexdisk/up.txt` → `sudo -u $U $YD sync` → проверить на Диске. | `up.txt` удалён и на Диске (удаление синхронизируется в обе стороны). |
| **5.4** | ⬜ Б | Дать файлу `down.txt` отличаться с двух сторон: локально `echo LOCAL > …/down.txt`, на Диске изменить его же на `REMOTE`. Затем один `sudo -u $U $YD sync`. | Срабатывает `--conflict-resolve path1`: **локальная** версия (`LOCAL`) выигрывает и остаётся как `down.txt`. Удалённая версия **не теряется** — сохраняется рядом переименованной копией (напр. `down.txt..path2`/`down.txt.conflict1`). Проверить наличие переименованной копии локально и на Диске. |
| **5.5** | 🟫 С | Создать в папке мусор: `Thumbs.db`, `.DS_Store`, каталог `@eaDir/x.txt`, `#recycle/y.txt` (от `$U`) → `sudo -u $U $YD sync` → `lsf yandexdisk:` рекурсивно. | Ни `Thumbs.db`, ни `.DS_Store`, ни содержимое `@eaDir/`, `#recycle/`, `@tmp/`, `#snapshot/` **не уходят** на Диск (excludes в bisync). |
| **5.6** | ⬜ Б | Включить опцию: дописать в `$CFG` строку `clean_thumbs="1"`. Убедиться, что локально есть `Thumbs.db` и `@eaDir/`. `sudo -u $U $YD sync` → проверить локальную папку. | После sync локальные `Thumbs.db`/`.DS_Store` **удалены** (одноразовая чистка внутри sync). Каталог `@eaDir/` **остаётся нетронутым** (его трогать нельзя — за ним следит индексатор Synology). Вернуть `clean_thumbs="0"` по завершении. |

---

## 7. РАЗДЕЛ 6 — Команды обёртки `yandex-disk`

**Предусловие:** Разделы 3–4 пройдены.

| # | Тип | Шаги | Ожидаемый результат |
|---|---|---|---|
| **6.1** | 🟫 С | `sudo -u $U $YD version` | Печатает реальную версию rclone (эта команда всегда вызывает бинарник напрямую, кэш не используется). |
| **6.2** | 🟫 С | `sudo -u $U $YD rclone lsd yandexdisk:` | Прокидывает аргументы в `rclone` с конфигом пакета: выводит список каталогов на Диске. Ошибок конфигурации нет. |
| **6.3** | ⬜ Б | `sudo -u $U $YD start; echo $?` и `sudo -u $U $YD stop; echo $?` | Обе печатают сообщение «No resident daemon in this build…» и завершаются с **rc=0** (no-op для совместимости). |
| **6.4** | ⬜ Б | `sudo -u $U $YD bogus; echo "rc=$?"` | Печатает `Usage: yandex-disk {sync\|status\|setup\|version\|rclone <args...>}`, **rc=1**. |
| **6.5** | ⬜ Б | Резерв-проверка `resolve_remote`: временно изменить `remote="нет-такого:"` в `$CFG` (remote в `rclone.conf` ровно один) → `sudo -u $U $YD status`. Затем вернуть `remote="yandexdisk:"`. | Строка `Удалённый ресурс:` показывает реально существующий единственный remote (`yandexdisk:`), а не отсутствующее имя — сработала авто-подстановка единственного remote. |
| **6.6** | ⬜ Б | Имитация перекрытия: `sudo -u $U sh -c "$YD sync & $YD sync & wait"` (или запустить sync второй раз, пока идёт долгий первый). | Один прогон работает, второй немедленно печатает `sync already running, skip` и завершается с rc=0 (защита `flock` на `$PKG/var/sync.lock`). Гонок за общий кэш bisync нет. |
| **6.7** | ⬜ Б | Парсер конфига: дописать в `$CFG` `dir="/volume1/yandexdisk"   # рабочая папка` (комментарий **через пробел**) → `sudo -u $U $YD status`. | `Локальная папка:` = `/volume1/yandexdisk` — инлайн-комментарий после пробела корректно отбрасывается, кавычки/пробелы триммятся. (Комментарий **без** пробела перед `#` испортит значение — оставлять `#` только через пробел.) |

---

## 8. РАЗДЕЛ 7 — Подсистема логов (поведение и устойчивость)

**Предусловие:** Разделы 4–5 пройдены (есть рабочий `rclone.log` и история).

| # | Тип | Шаги | Ожидаемый результат |
|---|---|---|---|
| **7.1** | ⬜ Б | Раздуть лог: `sudo -u $U sh -c 'head -c 1100000 /dev/zero \| tr "\0" "x" > $LOGD/rclone.log'` → `sudo -u $U $YD sync` → `ls -l $LOGD/rclone.log*`. | Появился бэкап `rclone.log.1` (старое содержимое), а `rclone.log` начат заново и компактен. Ротация срабатывает при >1 МБ (один бэкап, ~2× потолок) — лог не растёт бесконечно на диске 1 ГБ. |
| **7.2** | 🟫 С | `cat $LOGD/status_history.log` | Записи — блоки вида `[дата-время]` + строка `Последняя синхронизация: …`. После нескольких sync с разным временем видно несколько блоков; одинаковые подряд состояния **не дублируются**. |
| **7.3** | ⬜ Б | Записать сентинел: `echo 'rclone vSENTINEL' \| sudo -u $U tee $PKG/var/rclone.version` → в Центре пакетов **Остановить**, затем **Запустить** пакет → `ls $PKG/var/rclone.version`. | При старте `start-stop-status start` удаляет устаревший кэш версии (`rm -f`). Сразу после запуска сентинел-файла нет (до следующего `status`), затем он заполняется актуальной версией — корректный подхват версии после обновления пакета. |
| **7.4** | ⬜ Б | Single-instance heartbeat: при запущенном пакете выполнить `sudo -u $U env YD_PKG_DIR=$PKG $PKG/scripts/yandex-logger; echo "rc=$?"`. | Второй экземпляр немедленно завершается с **rc=1** (страж единственного процесса — `ps … grep '[y]andex-logger'`). Дубля heartbeat нет. |
| **7.5** | ⬜ Б ⏱ | Авто-recover потери базы: остановить расписание; удалить кэш листингов bisync (`rm -f $PKG/home/.cache/rclone/bisync/*`); `sudo -u $U $YD sync`. | В выводе/`rclone.log` фигурирует `Must run --resync` → срабатывает само-восстановление: печатается `[auto-recover] prior listings lost — re-running with --resync`, прогон завершается **rc=0**, `$STATE` = `idle\|…\|ok`. |

---

## 9. РАЗДЕЛ 8 — Пользовательский интерфейс (UI)

**Предусловие:** есть данные в `status_history.log` и `rclone.log` (после Раздела 5).

| # | Тип | Шаги | Ожидаемый результат |
|---|---|---|---|
| **8.1** | ⬛ Ч | Открыть приложение → вкладка **«История»**. | Содержимое совпадает с `status_history.log` (история статусов синхронизации). Под шапкой вкладок видна строка-подпись «Краткая история состояний синхронизации». |
| **8.2** | ⬛ Ч | Вкладка **«Журнал синхронизации»**. | Показаны последние строки реального лога bisync (`rclone.log`, до 300 строк). При ошибке `rc=N` здесь видна причина. |
| **8.3** | ⬛ Ч | Открыть вкладку **«История»**; затем переключиться на «Статус» и «Журнал синхронизации». | Красная кнопка **«Очистить»** видна **постоянно** в правой части шапки вкладок (прокрутка не нужна) и **только** на вкладке «История»; на «Статус»/«Журнал синхронизации» её нет. |
| **8.4** | 🟫 С | Нажать **«Очистить»** → в диалоге `Вы действительно хотите очистить историю синхронизации?` подтвердить. Затем сравнить файлы. | `status_history.log` **очищен** (вкладка «История» пуста). `rclone.log` (вкладка «Журнал синхронизации») **не тронут** — диагностический лог сохраняется. |
| **8.5** | ⬛ Ч | Переключение между всеми тремя вкладками; на время загрузки. | При смене вкладки кратко показывается «Загрузка…», затем контент. Окно ~800×500, поддерживает изменение размера; текст в области контента выделяется/копируется. |
| **8.6** | 🟫 С | В адресной строке браузера (с активной сессией DSM) открыть `/webman/3rdparty/YandexDisk/scripts/status.cgi`. | Возвращается `text/plain` с датой и выводом `yandex-disk status` (CGI работает под сессией DSM). |

---

## 10. РАЗДЕЛ 9 — Интеграция с Планировщиком задач

**Предусловие:** Раздел 4 пройден.

| # | Тип | Шаги | Ожидаемый результат |
|---|---|---|---|
| **9.1** | ⬛ Ч | Панель управления → Планировщик задач → Создать → Запланированная задача → Пользовательский сценарий. Имя `YandexDisk sync`, пользователь **root**, команда:<br>`sudo -u sc-yandexdisk /usr/local/bin/yandex-disk sync`. Сохранить → выделить задачу → **Выполнить**. | Задача создаётся и запускается без ошибок. После выполнения `cat $STATE` обновлён (новое время, `idle/ok`), в `rclone.log` — свежие строки. |
| **9.2** | 🟫 С ⏱ | Включить расписание (каждые 5 минут). Создать локально новый файл, подождать ≥5 минут, **ничего не запуская вручную**. | Файл уезжает на Диск автоматически; `$STATE` и `status_history.log` обновляются по расписанию — периодическая синхронизация работает без резидентного демона. |
| **9.3** | ⬜ Б | Не отключая частое расписание, сразу после старта прогона проверить, нет ли двойных процессов bisync: `ps -eo cmd \| grep '[r]clone bisync'`. | Не более одного активного `rclone bisync` одновременно — перекрывающийся запуск Планировщика отсекается `flock` (см. 6.6). |

---

## 11. РАЗДЕЛ 10 — Жизненный цикл пакета

| # | Тип | Шаги | Ожидаемый результат |
|---|---|---|---|
| **10.1** | ⬛ Ч | Центр пакетов → **Остановить** Yandex Disk (ARM). Затем `ps -eo cmd \| grep '[y]andex-logger'`. | Статус пакета → **«Остановлено»**. Процесс `yandex-logger` завершён (`start-stop-status stop` бьёт строго по `/scripts/yandex-logger`, посторонние процессы не задеты). |
| **10.2** | ⬜ Б | `sudo $PKG/scripts/start-stop-status status; echo "rc=$?"` в остановленном состоянии, затем после **Запустить** — снова. | В остановленном состоянии **rc=3**; после запуска heartbeat поднят, **rc=0** (это и есть признак «Запущено» для DSM/systemd). |
| **10.3** | 🟫 С ⏱ | (Опционально) Перезагрузить NAS. После загрузки — статус пакета и `ps … yandex-logger`. | Пакет автозапускается, heartbeat работает, статус «Запущено». Расписание синхронизации продолжает действовать. |
| **10.4** | 🟫 С | Перед обновлением зафиксировать токен/конфиг: `sudo -u $U cat $RCONF` и `cat $CFG`. Установить поверх более новый `.spk` (обновление), если доступен; иначе пропустить. | Обновление проходит; `rclone.conf` (OAuth-токен) и `config.cfg` в `home/` **сохраняются** — повторная авторизация и первый `--resync` не нужны; метка `$PKG/var/.bisync_resynced` на месте. Сохранность данных обеспечивают скрипты `preupgrade`/`postupgrade` (бэкап в `$SYNOPKG_TEMP_UPGRADE_FOLDER` + восстановление, если данные не пережили обновление). Кэш версии rclone сбрасывается и подхватывает новую версию (см. 7.3). |
| **10.5** | 🟫 С | Центр пакетов → **Удалить**. Затем `ls -l $YD $RCLONE 2>&1`, `ps -eo cmd \| grep '[y]andex-'`. | Пакет удалён, симлинки `/usr/local/bin/{yandex-disk,rclone}` убраны, фоновые процессы остановлены. (Учитывать политику DSM по сохранению пользовательских данных при удалении.) |

---

## 12. Очистка после тестирования
1. Удалить тестовые файлы локально и на Диске: `up.txt`, `down.txt`, переименованные конфликт-копии, `Thumbs.db`, `.DS_Store`, тестовые каталоги.
2. Вернуть рабочие значения `$CFG` (`clean_thumbs="0"`, корректный `dir`/`remote`).
3. Включить штатное расписание синхронизации в Планировщике.
4. Убедиться, что финальный `sudo -u $U $YD status` показывает `idle/ok`.

---

## 13. Сводная таблица покрытия

| Раздел | Область | Преобладающий тип |
|---|---|---|
| 1 | Установка, пользователь, симлинки, arch бинарника | ⬛ Ч / 🟫 С / ⬜ Б |
| 2 | Состояние до настройки, негатив «не настроено» | 🟫 С / ⬜ Б |
| 3 | OAuth, конфиг, права на папку | 🟫 С |
| 4 | Первый `--resync`, состояние, кэш версии | 🟫 С / ⬜ Б |
| 5 | Двусторонняя синхронизация, конфликты, excludes, clean_thumbs | 🟫 С / ⬜ Б |
| 6 | Команды обёртки, flock, парсер, resolve_remote, коды возврата | ⬜ Б |
| 7 | Логи: ротация, дедуп истории, кэш, single-instance, авто-recover | 🟫 С / ⬜ Б |
| 8 | UI: вкладки, очистка логов, CGI | ⬛ Ч / 🟫 С |
| 9 | Планировщик задач, отсутствие перекрытий | ⬛ Ч / 🟫 С / ⬜ Б |
| 10 | Жизненный цикл: stop/start/restart/upgrade/uninstall | ⬛ Ч / 🟫 С / ⬜ Б |

> **Критерий приёмки:** все тесты Разделов 1–9 — **PASS**; в Разделе 10 — отсутствие зависших процессов и
> корректные коды возврата `start-stop-status`. Любой **FAIL** в синхронизации (Раздел 5) или логах (Раздел 7)
> — блокирующий.

> **Вспомогательные скрипты (ускорители, не замена ручных тестов):** часть проверок Разделов 1–2 и 5
> автоматизирована в `test-on-nas-install.sh` (установка, arch, пользователь, конфиг) и
> `test-on-nas-functional.sh` (heartbeat, логи, тест загрузки/удаления файла). Их можно прогнать по SSH
> для быстрой регрессии; единичные ✗ разбираются соответствующим ручным тестом из этого плана.
